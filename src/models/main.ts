export type PromptIdea = {
  icon: React.ReactNode;
  label: string;
};

export type Project = {
  title: string;
  description: string;
  image: string;
  url: string;
};

export type Education = {
  title: string;
  description: string;
  image: string;
  courses: string[];
};

export type Language = {
  title: string;
  level: string;
  image: string;
};

export type Carreer = {
  title: string;
  description: string;
  image: string;
  achievments: string[];
};
