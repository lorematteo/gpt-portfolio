export type PromptIdea = {
  icon: React.ReactNode;
  label: string;
};

export type Project = {
  title: string;
  description: string;
  image: string;
  url: string;
};

export type Education = {
  title: string;
  description: string;
  image: string;
  courses: string[];
};
